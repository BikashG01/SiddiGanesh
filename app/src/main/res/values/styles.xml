<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="spinner_style">
        <item name="android:background">@drawable/spinner_layout</item>
        <!--<style name="MySpinnerLook" parent="@android:TextAppearance.Widget.TextView.SpinnerItem">
            <item name="android:textSize">22sp</item>
        </style>-->
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
    <!--Notitilebar theme-->
    <style name="NotTitle" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NotTitle.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="NotTitle.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
